<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:orientation="horizontal" android:padding="16dp" android:minHeight="64dp"
    android:gravity="center_vertical"
    android:background="?attr/selectableItemBackground" android:focusable="true">

    <TextView android:id="@+id/tvRouteType"
        android:layout_width="40dp" android:layout_height="40dp"
        android:textSize="22sp" android:gravity="center"
        android:importantForAccessibility="no"/>

    <TextView android:id="@+id/tvRouteNumber"
        android:layout_width="64dp" android:layout_height="wrap_content"
        android:textSize="20sp" android:textStyle="bold" android:layout_marginStart="8dp"
        android:gravity="center"/>

    <TextView android:id="@+id/tvRouteName"
        android:layout_width="0dp" android:layout_height="wrap_content"
        android:layout_weight="1" android:textSize="15sp" android:layout_marginStart="8dp"
        android:maxLines="2"/>

    <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:text="›" android:textSize="24sp" android:alpha="0.4"
        android:importantForAccessibility="no"/>
</LinearLayout>